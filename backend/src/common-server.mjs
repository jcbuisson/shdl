import { expressX as expressX_, protect as protect_, EXError as EXError_ } from '@jcbuisson/express-x'

export const expressX = expressX_
export const protect = protect_
export const EXError = EXError_
